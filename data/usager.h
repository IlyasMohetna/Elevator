#ifndef USAGER_H
#define USAGER_H

typedef struct {
    int id;                // Unique identifier for the user
    int etage_depart;      // Origin floor
    int etage_arrivee;     // Destination floor
} Usager;

#endif // USAGER_H
