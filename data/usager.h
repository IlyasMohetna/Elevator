#ifndef USAGER_H
#define USAGER_H

// Structure représentant un usager
typedef struct {
    int id;                // Identifiant unique pour l'usager
    int etage_depart;      // Étage d'origine
    int etage_arrivee;     // Étage de destination
} Usager;

#endif // USAGER_H
